# Type Alias: WidgetWidth

> **WidgetWidth** = `"auto"` \| `"full"` \| `number`

`auto` (default) sizes to content. `full` spans the parent. A positive
number is an explicit pixel value that overrides both (and any game
preferredWidth).
