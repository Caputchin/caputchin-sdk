# Type Alias: LayoutSource

> **LayoutSource** = `"attr"` \| `"manifest"` \| `"default"`

Where the resolved layout came from, reported on the `layout-resolved`
 event: the embed `attr`, the game `manifest`, or the built-in `default`.
