#!/usr/bin/env sh
# Pre-commit gate: typecheck + tests across the workspace. Codified after the
# cycle-1/2/3 "ran tests before final edit" recurrence on slice-5+6. Skip with
# `git commit --no-verify` ONLY when you've already run `pnpm verify` manually
# AND the diff hasn't been touched since.
exec pnpm verify
