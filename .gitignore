node_modules/
dist/
coverage/
.turbo/

# Rust (kits/replay-rs + the cargo-generate template). target/ is build output;
# Cargo.lock is omitted for this library workspace (no binary), regenerated by CI.
target/
Cargo.lock
*.log
.DS_Store
.env
qa-notes.md
qa-notes/