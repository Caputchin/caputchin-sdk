# Function: toBytes()

> **toBytes**(`trace`): `Uint8Array`

Decode a trace to bytes; pass it through if it is already bytes.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `trace` | `string` \| `Uint8Array`\<`ArrayBufferLike`\> |

## Returns

`Uint8Array`
