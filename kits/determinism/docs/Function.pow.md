# Function: pow()

> **pow**(`x`, `y`): `number`

x^y. Built as exp(y*log(x)) with sign/integer-exponent handling.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `x` | `number` |
| `y` | `number` |

## Returns

`number`
