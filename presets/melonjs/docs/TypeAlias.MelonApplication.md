# Type Alias: MelonApplication

> **MelonApplication** = `InstanceType`\<[`MelonNamespace`](TypeAlias.MelonNamespace.md)\[`"Application"`\]\>

A booted melonJS Application instance (carries `.world`, the camera, the renderer).
