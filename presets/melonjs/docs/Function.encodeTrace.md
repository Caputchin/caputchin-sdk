# Function: encodeTrace()

> **encodeTrace**\<`A`\>(`inputs`): `string`

Serialize recorded inputs into an opaque trace string.

## Type Parameters

| Type Parameter |
| ------ |
| `A` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `inputs` | readonly [`TickInput`](Interface.TickInput.md)\<`A`\>[] |

## Returns

`string`
