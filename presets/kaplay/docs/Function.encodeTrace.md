# Function: encodeTrace()

> **encodeTrace**(`events`): `string`

Serialize recorded events into an opaque trace string.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `events` | readonly [`RecordedEvent`](Interface.RecordedEvent.md)[] |

## Returns

`string`
