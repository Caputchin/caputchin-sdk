{
  "entry": "dist/{{project-name}}.js",
  "run": {
    "entry": "dist/run.js",
    "modules": [
      { "name": "{{project-name}}.wasm", "path": "dist/{{project-name}}.wasm", "type": "wasm" }
    ]
  },
  "preferred": { "width": 480, "height": 480 }
}
