# caputchin-js

Monorepo for `@caputchin/widget`, `@caputchin/game-sdk`, and `@caputchin/mcp` — the JavaScript/TypeScript client libraries for the Caputchin platform. Managed with pnpm workspaces.

```sh
pnpm i
pnpm -r build
```
